# Исключаемые папки
exclude = [
    ".venv",
    ".git",
    "__pycache__",
    ".vscode",
    "node_modules",
    ".eggs",
    "*.egg-info",
    ".mypy_cache",
    ".pytest_cache",
    ".ruff_cache",
]

# Максимальная длина строки
line-length = 88

# Настройки линтера
[lint]
select = ["E", "F", "W", "I"]
ignore = []

# Предупреждения о длине строки
[lint.pycodestyle]
max-line-length = 88

# Настройки форматирования
[format]
quote-style = "double"
indent-style = "space"

# Общие настройки
[lint.per-file-ignores]
# Можно добавить игноры для конкретных файлов
